{
    "changes": "* Upgrade Trigger.io platform to `v2.8.6`\n* Update Parse iOS SDK to `1.17.2`\n* Update module to use Bolts v1.10.0 which replaces usage of UIWebView with WKWebView",
    "dependencies": {
        "bolts": {
            "minimum_version": "1.10.0",
            "maximum_version": "1.10.0"
        }
    },
    "description": "Parse.com configuration to enable push notifications.\n\nThe parse module uses the Parse.com SDK to allow for native push notifications.",
    "min_platform_version": "v2.8.6",
    "namespace": "parse",
    "platform_version": "v2.8.6",
    "version": "3.4.0"
}
